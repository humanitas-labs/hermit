# 10 Inference Boundary

**Status:** in progress (policy, both seams, TUI, and tests landed in the working tree; harness scenarios S1, S3, S5, S7-S13 pending on `11`)

## 1. Goal

Every Hermit-managed model call must satisfy the same inference policy before transmitting context. Reuse the existing inference paths and add shared policy checks at their enforcement points. Do not add a new routing layer.

A supported inference path is complete only when policy enforcement and a corresponding test in `11` cover it. Any path not in the inventory below is unsupported and must be removed or gated off until it is inventoried.

## 2. Owned files

- New `packages/opencode/src/hermit/policy.ts`: endpoint classification, policy resolution, and the destination check.
- New `packages/opencode/src/hermit/AGENTS.md`: boundary notes.
- `packages/core/src/v1/config/config.ts`: `hermit.preset` and `provider.<id>.owner: "user"` (see 3.2); the config layering rule in 3.5.
- `packages/opencode/src/provider/provider.ts`: expose the resolved per-model endpoint and its class on `Provider.Model`; `resolveSDK` fetch wrapper is the enforcement point for V1.
- `packages/opencode/src/session/llm.ts`, `session/prompt.ts` lines 218-221, `session/compaction.ts`, subagent and agent-generation call sites: policy check before `streamText`.
- `packages/opencode/src/plugin/index.ts`: the `experimental.provider.small_model` hook output is subject to the same check.
- V2 runner and native-runtime paths (inventory in 3.0 names the files).
- `packages/tui/src` status line and `dialog-provider.tsx`: render the class.
- `packages/protocol` / SDK: class field on provider and model info; regenerate.

## 3. Design

### 3.0 Inference-path inventory

Traced in code at `785fed388e`. Every row is a place Hermit sends model context to a provider. "Endpoint" says where the destination URL comes from; "Enforcement" names the single seam the destination check in 3.2 must cover for that row. All V1 rows funnel through two seams, `resolveSDK` for the AI SDK runtime and `RequestExecutor` for the native runtime, so the check is implemented twice and tested per row.

| Path                            | Where                                                                                                                                                                                                                                                                                                  | Transport                | Endpoint                                                                                                                   | Enforcement                                                                                                                                                                                                                                                                                                                                        | Status      |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------ | -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| V1 primary turn, AI SDK runtime | `session/llm.ts:277-280` `streamText` with a language model from `Provider.getLanguage` → `resolveSDK` (`provider/provider.ts:1734`, `:1905`)                                                                                                                                                          | HTTP                     | `options.baseURL` from config, else `model.api.url` from the catalog, with `${VAR}` substitution (`provider.ts:1760-1780`) | `resolveSDK` fetch wrapper `provider.ts:1804`, which wraps any provider-supplied `customFetch`                                                                                                                                                                                                                                                     | inventoried |
| V1 primary turn, native runtime | `session/llm.ts:225-262` when `OPENCODE_EXPERIMENTAL_NATIVE_LLM`; `session/llm/native-runtime.ts:79-142` → `LLMClient.stream`                                                                                                                                                                          | HTTP                     | `provider.options.baseURL` or the route's canonical host (`packages/llm/src/route/endpoint.ts`)                            | `RequestExecutor` (`packages/llm/src/route/executor.ts:366`), constructed from the global `httpClient` node in `core/src/effect/app-node-platform.ts:10-16`. Falls back to the AI SDK runtime on `unsupported`, same destination class, logged at `llm.ts:255`                                                                                     | inventoried |
| V1 title generation             | `session/prompt.ts:217-233`: `title` agent model, else `getSmallModel`, else the session model; `small: true`                                                                                                                                                                                          | HTTP                     | as primary                                                                                                                 | as primary; the model choice is a separate policy input                                                                                                                                                                                                                                                                                            | inventoried |
| V1 compaction                   | `session/compaction.ts:358-361`: `compaction` agent model override, else the session model. Never the small model                                                                                                                                                                                      | HTTP                     | as primary                                                                                                                 | as primary                                                                                                                                                                                                                                                                                                                                         | inventoried |
| V1 subagents                    | `tool/task.ts:181-209` → `SessionPrompt.prompt` with the subagent's model or the parent's                                                                                                                                                                                                              | HTTP                     | as primary                                                                                                                 | as primary; child sessions inherit the policy, nothing to add                                                                                                                                                                                                                                                                                      | inventoried |
| Agent generation                | `agent/agent.ts:373-435`: `generateObject` or `streamObject` (OpenAI OAuth) on `provider.getLanguage(resolved)` with the default model                                                                                                                                                                 | HTTP                     | as primary                                                                                                                 | as primary (`resolveSDK`)                                                                                                                                                                                                                                                                                                                          | inventoried |
| Project copy name               | `httpapi/handlers/project-copy.ts:29-33`: `getSmallModel` of the default provider, else the default model, via `llm.stream`                                                                                                                                                                            | HTTP                     | as primary                                                                                                                 | as primary                                                                                                                                                                                                                                                                                                                                         | inventoried |
| Plugin small-model hook         | `provider.ts:1953-1957` triggers `experimental.provider.small_model`; only built-in implementer is `plugin/github-copilot/copilot.ts:355`. Output is a `ModelV2` in the same provider                                                                                                                  | HTTP                     | the substituted model's `api.url`                                                                                          | the substituted model goes through `getLanguage` → `resolveSDK`, so the check applies. Cross-provider substitution must be rejected explicitly in `getSmallModel`                                                                                                                                                                                  | inventoried |
| Provider-supplied fetch         | `provider.options.fetch` from built-in loaders (`google-vertex` `provider.ts:542`, `snowflake-cortex` `:945`) and auth plugins (`copilot.ts:102`, `codex.ts:333-365`)                                                                                                                                  | HTTP                     | the URL passed in by the SDK                                                                                               | `resolveSDK` wraps it as `customFetch` and sees the URL before delegating. A plugin fetch that ignores the URL is outside enforcement; plugins are trusted executables per the contract                                                                                                                                                            | inventoried |
| Codex WebSocket transport       | `plugin/openai/codex.ts:333` installs `OpenAIWebSocketPool.createWebSocketFetch` as the provider fetch when `experimentalWebSocketsEnabled` (`plugin/index.ts:62-73`, default on for `local`, `dev`, `beta` channels); `ws-pool.ts:234` opens `wss://` on `toWebSocketUrl(url)` and falls back to HTTP | WebSocket, HTTP fallback | `CODEX_API_ENDPOINT` `https://chatgpt.com/backend-api/codex/responses` (`codex.ts:12`)                                     | the `resolveSDK` wrapper sees the HTTP URL; the socket URL is derived from it inside the plugin. Under `private` and `trusted` this provider is third-party and rejected before the fetch is installed. Under `external` it is permitted as third-party. `ws.ts:85` supplies the HTTP proxy explicitly, so the recording proxy in `11` observes it | inventoried |
| V2 runner                       | `core/src/session/runner/llm.ts` → `LLMClient.stream(request)`; model from `SessionRunnerModel` (`runner/model.ts:181-200`)                                                                                                                                                                            | HTTP                     | `model.api.url` from `Catalog` (`runner/model.ts:97`, `core/src/catalog.ts:80-102`) mapped onto a route                    | `RequestExecutor`, same node as the native runtime                                                                                                                                                                                                                                                                                                 | inventoried |
| V2 compaction                   | `core/src/session/compaction.ts:202-204` `llm.stream` with the runner's model                                                                                                                                                                                                                          | HTTP                     | as V2 runner                                                                                                               | as V2 runner                                                                                                                                                                                                                                                                                                                                       | inventoried |
| Native LLM WebSocket transport  | `packages/llm/src/route/transport/websocket.ts`; `WebSocketExecutor` is optional on `TransportRuntime`                                                                                                                                                                                                 | WebSocket                | route endpoint                                                                                                             | no route in the tree selects it and `app-node-platform.ts` provides no `WebSocketExecutor`, so it is unreachable. Add a test that asserts this stays true                                                                                                                                                                                          | inventoried |

Not inference, listed so nobody re-audits them: `tool/websearch.ts` and `mcp-websearch.ts` (tool egress, plan `06`), the OAuth token refreshes in `codex.ts:118,136` and `copilot.ts:234,264` (credential egress to the provider's issuer, only when that provider is selected), and `catalog` fetches (plan `01`).

Consequences for the design below:

1. Two seams cover every row: the `resolveSDK` fetch wrapper and `RequestExecutor`. The policy module is called from both with the final URL.
2. Model selection for titles, compaction, and project-copy names never leaves the session's provider unless config or a plugin says so. The policy check is on the resolved endpoint, so those substitutions are covered without special cases, except cross-provider `small_model` substitution, which is rejected in `getSmallModel`.
3. The native runtime's fallback to the AI SDK runtime is not a destination change and is not a silent-fallback violation.

Each row gets a scenario in `11` or the "unreachable" assertion named above.

### 3.1 Classification

`policy.ts` exports `classify(endpoint: URL, config): "local" | "user" | "third-party"`. Classification is derived from the effective request endpoint, per request, not per provider ID. A provider whose models resolve to different `baseURL`s gets a class per model.

1. Loopback host (`127.0.0.0/8`, `::1`, `localhost`, literal, no DNS) is `local`. Configuration cannot label a remote endpoint as local.
2. Any other host defaults to `third-party`.
3. The user may explicitly designate a remote endpoint `user` via `provider.<id>.owner: "user"` in user-level config. That designation describes ownership, not locality, and cannot produce `local`.

No DNS. No suffix matching. A private-range IP is `third-party` unless the user designates it `user`.

Loopback classification assumes the local inference service is trusted and does not forward context remotely. That assumption is stated in the acceptance contract.

### 3.2 Destination check

Before sending context, validate the effective inference destination against the active policy (3.4) and the user's authorized endpoint configuration. An unchanged provider ID is not sufficient authorization; the check runs on the resolved URL.

- Fail closed on an unauthorized destination with a tagged error the UI can show. Never automatically fall back to another provider or endpoint.
- Reject HTTP redirects from `local` and `user` endpoints in the initial implementation. Third-party providers may legitimately redirect; for them, the redirect target must classify the same and stay on the same registrable host, else fail. A provider that needs cross-host redirects needs an explicit design and tests before support.
- Title, compaction, small-model, subagent, agent-generation, and plugin small-model hook results must pass the same check. Explicit secondary-model configuration is permitted only inside the authorized boundary.
- The Codex WebSocket transport enforces the same check on its connect URL or is unavailable under `private` and `trusted` presets.
- Retry logic (`session/retry.ts`) must never select a different endpoint. Verify by reading it; upstream currently retries the same model only.

### 3.3 Visibility

TUI status line shows `LOCAL`, `USER`, or `THIRD PARTY` next to the model name, colored by the Hermit theme (green, orange, red). The model picker groups models by class. `hermit models` prints the class per model.

### 3.4 Presets

`hermit.preset`:

- `private` permits only `local` inference.
- `trusted` permits `local` and explicitly designated `user` inference.
- `external` additionally permits `third-party` inference.

Default is `private`. Remote use requires deliberate user configuration or selection that updates the applicable policy.

A catalog entry or stored credential may exist without being usable under the current policy. Reject forbidden model selections and requests; do not reject startup merely because credentials for another provider exist. This lets a user keep a Fireworks key while running a genuinely local session.

Validate the policy at startup (report which configured models are usable) and enforce it on every inference request.

### 3.5 Policy ownership

Privacy policy and endpoint authorization come from user-level configuration outside the repository, or from a deliberate user action in the UI. Project configuration may narrow the policy but cannot weaken it. Concretely, project config cannot:

- authorize a new inference destination
- change an endpoint's ownership classification
- enable remote secondary inference
- raise the preset

Credentials discovered in the environment make authentication available; their presence alone does not authorize context transmission.

Implement as a merge rule in config loading: `hermit.*` and `provider.<id>.owner` are read from user-level config only; a project value for those keys is ignored with a logged warning, except a project preset that is stricter than the user's.

## 4. Verification

Landed:

- `packages/core/test/hermit-policy.test.ts`: `classify` over IPv4 and IPv6 loopback, `localhost`, mixed case, userinfo tricks (`http://localhost@evil/`), private ranges, `0.0.0.0`; presets; redirect rules; `fromConfig` including `owner` without a `baseURL` and templated URLs; `narrow`; `guardFetch` against a loopback server including a refused redirect and a per-request policy read.
- `packages/opencode/test/hermit/boundary.test.ts`: the V1 seam through `Provider.getLanguage` and `streamText` against a fake server listening on `0.0.0.0`, reached as loopback (local) and as a non-loopback literal (third-party). Covers the private default, a project config trying to raise the preset, user-config `external`, user-config `trusted` with `owner: "user"`, a project config trying to set `owner`, a local endpoint that redirects, and a `small_model` on a remote provider during a private session.
- `packages/core/test/hermit-executor.test.ts`: the native and V2 seam through `RequestExecutor`, including the narrowed process policy.

Remaining: the plugin small-model hook returning a cross-provider model is rejected in `getSmallModel` by code review only; a plugin fixture test can be added with `11`. Integration in `11`: scenarios S1, S3, S5, S7-S12.

`session/retry.ts` was read: it retries the same model and never selects another endpoint.

## 5. Divergence log

| Upstream file                                                                                    | Change                                                                                                                                                                                                                                     | Reason                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/hermit/policy.ts` (new)                                                       | classification, presets, destination check, redirect rule, `guardFetch`, process-global narrowing policy, `fetchLayer`                                                                                                                     | the policy module lives in core rather than `packages/opencode/src/hermit` as first planned, because the native executor in core must call it and core cannot import opencode |
| `packages/core/src/v1/config/config.ts`                                                          | `hermit.preset` key                                                                                                                                                                                                                        | preset configuration                                                                                                                                                          |
| `packages/core/src/v1/config/provider.ts`                                                        | `provider.<id>.owner: "user"`                                                                                                                                                                                                              | user-owned endpoint designation                                                                                                                                               |
| `packages/core/src/effect/app-node-platform.ts`                                                  | `requestExecutor` gets its own `FetchHttpClient` with the guarded `Fetch`; no longer depends on the shared `httpClient` node                                                                                                               | native runtime and V2 runner seam; tools keep the unguarded client                                                                                                            |
| `packages/opencode/src/config/config.ts`                                                         | `stripPolicy` drops `hermit` and `owner` from project-scope sources except a stricter preset; `HermitPolicy.narrow` after load                                                                                                             | policy ownership rule in 3.5                                                                                                                                                  |
| `packages/opencode/src/provider/provider.ts`                                                     | `endpoint` helper extracted from `resolveSDK`; fetch wrapper goes through `guardFetch`; `Model.boundary` and `Model.permitted`; `State.policy`; classification pass and startup log; `getSmallModel` ignores cross-provider plugin results | V1 seam, visibility, 3.2 small-model rule                                                                                                                                     |
| `packages/llm/src/route/executor.ts`                                                             | `TransportError` mapping keeps the wrapped fetch error's message                                                                                                                                                                           | the refusal reason reaches the user on the native path                                                                                                                        |
| `packages/opencode/src/cli/cmd/models.ts`                                                        | prints the trust class and `not permitted`                                                                                                                                                                                                 | 3.3                                                                                                                                                                           |
| `packages/tui/src/context/local.tsx`, `component/prompt/index.tsx`, `component/dialog-model.tsx` | footer label `LOCAL`, `USER`, `THIRD PARTY` colored success, warning, error; picker footer and disabled state                                                                                                                              | 3.3. Colors use the current theme tokens; the Hermit theme in `12` N7 restyles them                                                                                           |
| `packages/sdk/js/src/v2/gen/types.gen.ts`, `packages/sdk/openapi.json`                           | regenerated                                                                                                                                                                                                                                | `Model` schema gained two optional fields                                                                                                                                     |
| `packages/opencode/src/hermit/AGENTS.md` (new)                                                   | boundary notes for agents                                                                                                                                                                                                                  | 2                                                                                                                                                                             |

## 6. Cross-region notes

- V2 core config (`packages/core/src/config.ts`) has no `hermit` key. The V2 runner is covered by the process-global policy that V1 config loading narrows, so a V2-only process without a V1 instance load runs under the private default. Revisit when V2 config is the primary config.
- `OPENCODE_CONFIG_CONTENT` is treated as project scope by `pluginScopeForSource`, so policy keys in it are stripped. The egress harness in `11` must set the preset through the user config directory, not that variable.
- The Codex WebSocket transport needs no special code: its HTTP URL is `chatgpt.com`, third-party under every preset below `external`, and the check runs before the plugin fetch that opens the socket. `11` S13 should confirm on the built binary.
